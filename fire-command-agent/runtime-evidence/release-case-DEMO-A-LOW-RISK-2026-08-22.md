# 固定案例验收：DEMO-A-LOW-RISK

> ⚠️ 历史证据（2026-08-22）：以下内容按当日记录保留，sceneId=`463601914351104000` 为当日旧值。仅供历史追溯，不能作为当前计划、现场预演或验收依据；当前正式 sceneId 为 `477747327523254272`。

## 结果

`BLOCKED`

## 固定输入

- 建筑：五矿国际广场
- 场景：`463601914351104000`
- 目标：`1F / 商铺1`
- `room_out_instance_id`：`e73835e6-9f67-4908-96ca-5a1c21f050bd`
- 火灾类型：普通可燃物火灾（演示输入）
- 被困人数：0
- 过火面积：8 平方米
- 路线条件：主路线可用

## 实际回执

通过主工程在统一预案服务 3000 端口发起 `rescue-plan.generate_plan`，HTTP 返回 200 并创建临时草稿；每次重试产生不同的临时 planId，因此未把它作为发布版本证据。关键业务结果为：

- 生命周期：`draft`
- I-V：`pending_manual_review`，无等级建议
- 力量：`pending_manual_review`，0 个单位
- 路线/水源：`pending_manual_review`，无主路线、备用路线和水源
- 三维：11 个步骤结构存在，但状态为 `pending_manual_review`
- 阻断：空间定位未取得可核验回执；策略 Skill 超时

报告保留脱敏后的状态字段；具体一次性 planId 以服务日志和对应 JSON 记录为准，避免将临时重试编号当作发布版本。

## 门槛判断

低风险目标的“低等级、最小力量、真实主路线和水源”均未形成可核验结果，案例不通过。
